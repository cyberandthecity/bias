import Scene from "@/components/scenes/scene"

const Home = () => {
	return (
		<div style={{ position: "absolute", width: "100%", height: "100%", margin: 0, padding: 0, top: 0, left: 0 }}>
			{<Scene />}
		</div>
	)
}

export default Home
